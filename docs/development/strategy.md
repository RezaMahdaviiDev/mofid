# راهنمای استراتژیک و فلسفه پروژه

**مخاطب:** هوش مصنوعی پروژه، توسعه‌دهنده (رضا)  
**هدف:** ثبت تصمیمات استراتژیک کلیدی و فلسفه‌ی پشت پروژه برای حفظ یکپارچگی و جهت‌گیری آینده.  
**نسخه:** 1.0  
**آخرین به‌روزرسانی:** 2025-01-08

---

## مقدمه: از ربات تا سیستم معاملاتی

این پروژه از یک ابزار شخصی برای سرخطی زدن فراتر رفته و به یک **سیستم معاملاتی کامل** در حال تبدیل است. موفقیت ما در گرو درک این سه حقیقت است:
1. این پروژه شخصی است و برای **خدمت به معامله‌گر** (رضا) طراحی شده، نه برای فروش.
2. **مستندسازی کامل** یک مزیت استراتژیک است که به هوش مصنوعی کمک می‌کند تا سریع‌تر و دقیق‌تر عمل کند.
3. ما نه تنها یک ابزار می‌سازیم، بلکه در حال ساختن یک **زیرساخت برای استراتژی‌های معاملاتی آینده** هستیم.

---

## ۱. فلسفه "تثلیث معامله‌گر" (The Trader's Trinity)

هر سیستم معاملاتی موفق بر سه ستون استوار است. پروژه ما باید هر سه را پوشش دهد:

### سرعت (Speed)
این "دست‌های" ماست. ربات باید بتواند سفارشات را در سریع‌ترین زمان ممکن ثبت کند. **ما در این بخش به موفقیت رسیده‌ایم.** مدل‌های مختلف (از UI تا API) ما سرعت‌های زیر ۱۰۰ میلی‌ثانیه را نیز هدف قرار داده‌اند.

### اطلاعات (Information)
این "مغز متفکر" ماست. پروژه آینده "شرط‌گذاری" دقیقاً برای همین است. باید بتوانیم داده‌های لحظه‌ای بازار را تحلیل کرده و سیگنال‌های خرید/فروش تولید کنیم.

### استراتژی (Strategy)
این "روح" معامله‌گر است. شامل مدیریت ریسک، تعیین حد ضرر، و قوانین معاملاتی است. این بخش بیشتر از کدنویسی به روانشناسی و نظم نیاز دارد.

**تصمیم استراتژیک:** در حال حاضر، ما روی ستون **سرعت** مسلط هستیم. تمرکز بعدی باید به سمت **اطلاعات** (از طریق API) و **استراتژی** (از طریق قوانین قابل تنظیم) حرکت کند.

---

## ۲. تصمیم کلیدی: پایداری در اولویت بر سرعت

در معامله واقعی، یک سفارش ناموفق به دلیل عدم پایداری، بسیار گران‌تر از چند میلی‌ثانیه تأخیر است.

### سناریوی نامطلوب
ربات با سرعت 50ms سفارشی را ارسال می‌کند اما به دلیل خطای API ثبت نمی‌شود. معامله‌گر فکر می‌کند سفارش رفته و فرصت را از دست می‌دهد.

### سناریوی مطلوب
ربات با سرعت 300ms (که همچنان فوق‌العاده سریع است) سفارش را با نرخ موفقیت 99.9% ثبت می‌کند. معامله‌گر از انجام معامله مطمئن است.

**تصمیم نهایی:** **پایداری (Reliability) اولویت اول است.**

**اقدام عملی:**
- تمرکز اصلی روی **Model 5.1 (API Standard)** با retry logic کامل باشد.
- از Model 6 (Ultra) برای موارد خاص و با آگاهی از ریسک استفاده شود.
- هدف نهایی، رسیدن به نرخ موفقیت 99.9%+ در درخواست‌های API است.

---

## ۳. استراتژی داده‌ها: کافی است، اما یک ماموریت ویژه داریم

اطلاعات API که از کارگزاری مفید استخراج کردیم برای 80-90% معاملات روزمره **کافی است**.

### آنچه داریم و کافی است:
- `POST /core/api/v2/order` (ثبت سفارش خرید/فروش)
- `GET /core/api/order` (دریافت وضعیت سفارشات)
- `GET /ms/api/MarketSheet/order-place` (جایگاه در صف)
- مکانیزم استخراج خودکار توکن (`EasyTraderAPIClient`)

### آنچه ممکن است کم باشد (ماموریت ویژه):
**API لغو سفارش (Order Cancellation):** این حیاتی‌ترین API گمشده است. برای مدیریت ریسک، لغو سفارشات اشتباه ضروری است.

**تصمیم نهایی:** نیازی به رکوردگیری مجدد و گسترده نیست.

**اقدام عملی (جراحی دقیق):**
1. یک سفارش آزمایشی ثبت کن که اجرا نشود.
2. با استفاده از خود سایت مفید، آن را لغو کن.
3. همزمان، تب Network در DevTools را باز کن تا دقیقاً کدام API (`DELETE` یا `POST` با پارامتر خاص) فراخوانی می‌شود را شناسایی کنی.
4. نتایج را در `docs/api/easytrader-api-spec.md` مستند کن.

---

## ۴. استاد شدن در Rate Limiting: یک دفاع سه‌لایه

کارگزاری مفید محدودیت‌های نرخ (Rate Limiting) دارد. عبور از این محدودیت‌ها منجر به مسدود شدن موقت API می‌شود. ما باید یک سیستم دفاعی هوشمند داشته باشیم.

### لایه ۱: پیشگیری (Prevention)
- **الگوریتم Token Bucket:** برای کنترل نرخ درخواست‌ها در طول زمان. این اجازه می‌دهد گاهی به صورت بسته درخواست ارسال کنیم، اما میانگین نرخ از حد مجاز عبور نکند.
- **صف هوشمند (Intelligent Queuing):** اگر درخواستی به دلیل Rate Limit نتواند ارسال شود، آن را در صف قرار داده و به محض امکان، ارسال کند. این تضمین می‌کند که هیچ معامله‌ای به دلیل Rate Limit از دست نرود.

### لایه ۲: شناسایی (Detection)
- باید به دنبال کد وضعیت `429 Too Many Requests` و پیام‌های خطای خاصی که مفید در پاسخ‌های JSON برمی‌گرداند، باشیم.

### لایه ۳: بازیابی (Recovery)
- **تکنیک Exponential Backoff:** در صورت بروز خطا، قبل از تلاش مجدد به صورت نمایی (1s, 2s, 4s, ...) صبر کن. این به سرور نشان می‌دهد که ما خطا را درک کرده‌ایم.

**اقدام عملی:**
1. اجرای تست **ACET (API Comprehensive Endurance Test)** برای پیدا کردن اعداد واقعی Rate Limit (مثلاً چند درخواست در ثانیه؟).
2. پیاده‌سازی سیستم سه‌لایه بالا بر اساس نتایج تست ACET.

---

## ۵. اقدامات فوری و اولویت‌بندی شده

برای حفظ حرکت رو به جلوی پروژه، این‌ها اولویت‌های فعلی هستند:

### اولویت ۱: تمرکز بر پایداری Model 5.1
اطمینان از اینکه این مدل به عنوان ستون فقرات سیستم API ما، نرخ موفقیت بالایی دارد.

### اولویت ۲: اجرای ماموریت "جراحی دقیق"
پیدا کردن و مستندسازی API لغو سفارش. این یک مزیت رقابتی بزرگ برای مدیریت ریسک است.

### اولویت ۳: اجرای تست ACET
به دست آوردن داده‌های واقعی درباره Rate Limiting و TTL توکن. این داده‌ها برای بهینه‌سازی ضروری هستند.

### اولویت ۴: پیاده‌سازی Rate Limiter پیشرفته
بر اساس استراتژی سه‌لایه و نتایج تست ACET.

---

## 💭 خلاصه فلسفه

این پروژه در مسیر درستی قرار دارد. با تمرکز بر این استراتژی‌ها، ما در حال ساختن یک ابزار معاملاتی شخصی قدرتمند، قابل اعتماد و هوشمند هستیم.

### اصول کلیدی:
1. ✅ **پایداری بر سرعت** - Reliability > Speed
2. ✅ **مستندسازی کامل** - کمک به AI و حفظ دانش
3. ✅ **تثلیث معامله‌گر** - سرعت، اطلاعات، استراتژی
4. ✅ **دفاع سه‌لایه** - پیشگیری، شناسایی، بازیابی

---

## 🔗 لینک‌های مرتبط

- [بحث‌ها و چالش‌ها](../discussions/challenges-and-questions.md)
- [بیوگرافی توسعه‌دهنده](../BIOGRAPHY.md)
- [دفترچه گزارش پیشرفت](journal.md)
- [Best Practices](best-practices.md)
- [مستندات API](../api/easytrader-api-spec.md)

---

**نسخه:** 1.0  
**آخرین به‌روزرسانی:** 2025-01-08
