# راهنمای استفاده از داشبورد

## 🚀 شروع سریع

### 1. اجرای داشبورد

```bash
npm run dashboard
```

### 2. باز کردن در مرورگر

به آدرس زیر بروید:
```
http://localhost:3000
```

---

## 📋 ویژگی‌ها

### فرم سفارش (خرید / فروش)

- **نماد**: نام نماد مورد نظر (مثلاً: زر)
- **قیمت**: قیمت خرید/فروش (مثلاً: 590000)
- **تعداد**: تعداد سهم (مثلاً: 2)
- **نوع سفارش**: انتخاب خرید یا فروش
  - 🟢 خرید: برای خرید سهم
  - 🔴 فروش: برای فروش سهم
- **مدل خرید**: انتخاب یکی از مدل‌ها:
  - مدل ۱: Standard (۹۶۰ms) - امن و پایدار
  - مدل ۴: Ultra (۲۰۲ms) 🏆 - سریع‌ترین
  - مدل ۵: API (۳۶۴ms) 🔮 - استفاده از API مستقیم
- **نمایش مرورگر**: برای دیباگ (تیک بزنید تا مرورگر قابل مشاهده باشد)

**نکته**: متن دکمه به صورت خودکار بر اساس نوع سفارش تغییر می‌کند:
- 🚀 خرید - برای سفارشات خرید
- 💰 فروش - برای سفارشات فروش

### نمایش وضعیت

- **آماده**: آماده برای سفارش
- **در حال پردازش**: در حال ارسال درخواست
- **موفقیت**: ✅ سفارش با موفقیت انجام شد
- **خطا**: ❌ پیام خطا

### 💰 نمایش موجودی نقدی

بخش جدید برای نمایش موجودی نقدی و تغییرات:

- **موجودی فعلی**: موجودی نقدی فعلی حساب
- **تغییر موجودی**: نمایش تغییر موجودی پس از هر معامله
  - 🟢 افزایش: موجودی افزایش یافته (برای فروش موفق)
  - 🔴 کاهش: موجودی کاهش یافته (برای خرید موفق)
  - ⚪ بدون تغییر: موجودی تغییر نکرده
- **اعتبارسنجی**: بررسی تطابق تغییر موجودی با نوع معامله
  - ✅ معتبر: تغییر موجودی با نوع معامله تطابق دارد
  - ⚠️ هشدار: تغییر موجودی صفر یا نامشخص
  - ❌ نامعتبر: تغییر موجودی با نوع معامله تطابق ندارد

### 📜 تاریخچه معاملات

- نمایش تمام معاملات (خرید و فروش)
- شامل اطلاعات زیر:
  - **نوع معامله**: 🟢 خرید یا 🔴 فروش
  - **نماد**: نام نماد معامله شده
  - **قیمت**: قیمت معامله
  - **تعداد**: تعداد سهم
  - **زمان**: زمان انجام معامله
  - **مدت زمان**: مدت زمان اجرای معامله (میلی‌ثانیه)
  - **تغییر موجودی**: تغییر موجودی پس از معامله
  - **وضعیت اعتبارسنجی**: وضعیت تطابق تغییر موجودی

- ذخیره در localStorage مرورگر
- نمایش 10 معامله آخر

---

## 🔐 لاگین

### اولین بار

1. کلیک روی دکمه "🔐 لاگین"
2. مرورگر باز می‌شود (یا مخفی است اگر debug غیرفعال باشد)
3. در مرورگر باز شده، به صورت دستی لاگین کنید
4. بعد از 30 ثانیه، session ذخیره می‌شود
5. در معاملات بعدی نیازی به لاگین نیست

### نکات مهم

- Session در `.user-data/easy/` ذخیره می‌شود
- اگر session منقضی شد، دوباره لاگین کنید
- برای دیباگ، تیک "نمایش مرورگر" را بزنید

---

## 🎯 گزینه Debug

### حالت عادی (Headless)

- مرورگر مخفی است
- سریع‌تر
- مصرف منابع کمتر
- مناسب برای استفاده واقعی

### حالت Debug

- مرورگر قابل مشاهده است
- می‌توانید مراحل را ببینید
- مناسب برای دیباگ و تست
- کندتر از حالت عادی

**نحوه فعال‌سازی:**
- تیک "نمایش مرورگر (برای دیباگ)" را در فرم سفارش بزنید
- یا هنگام لاگین، تیک را بزنید

---

## 🐛 عیب‌یابی

### مشکل: سرور اجرا نمی‌شود

**راه‌حل:**
```bash
# بررسی پورت 3000
netstat -ano | findstr :3000

# اگر پورت اشغال است، در server.ts پورت را تغییر دهید
```

### مشکل: خطای 401 در سفارش

**علت**: Session منقضی شده یا لاگین نشده

**راه‌حل:**
1. کلیک روی دکمه "لاگین"
2. دوباره لاگین کنید
3. سفارش را دوباره امتحان کنید

### مشکل: مرورگر باز نمی‌شود

**راه‌حل:**
```bash
# نصب مرورگرهای Playwright
npx playwright install chromium
```

### مشکل: خطای "Cannot find module"

**راه‌حل:**
```bash
# کامپایل مجدد
npm run build

# نصب وابستگی‌ها
npm install
```

### مشکل: موجودی "نامشخص" نمایش داده می‌شود

**علت**: ممکن است selector موجودی تغییر کرده باشد یا صفحه به درستی لود نشده باشد

**راه‌حل:**
1. اطمینان حاصل کنید که صفحه کارگزاری به درستی لود شده است
2. بررسی کنید که لاگین انجام شده است
3. در صورت نیاز، با حالت debug بررسی کنید

---

## 📊 API Endpoints

### POST /api/buy

**Request:**
```json
{
  "symbol": "زر",
  "price": 590000,
  "quantity": 2,
  "model": "4",
  "side": "buy",
  "debug": false
}
```

**Response (موفق):**
```json
{
  "success": true,
  "message": "خرید با موفقیت انجام شد",
  "duration": 763,
  "order": {
    "symbol": "زر",
    "price": 590000,
    "quantity": 2,
    "side": "buy"
  },
  "asset": {
    "balanceBefore": 15000000,
    "balanceAfter": 14882000,
    "change": -118000,
    "changeType": "decreased",
    "validationStatus": "valid",
    "validation": {
      "isValid": true,
      "message": "تغییر موجودی معتبر است",
      "severity": "success"
    }
  }
}
```

**Response (فروش موفق):**
```json
{
  "success": true,
  "message": "فروش با موفقیت انجام شد",
  "duration": 765,
  "order": {
    "symbol": "زر",
    "price": 590000,
    "quantity": 2,
    "side": "sell"
  },
  "asset": {
    "balanceBefore": 14882000,
    "balanceAfter": 15000000,
    "change": 118000,
    "changeType": "increased",
    "validationStatus": "valid",
    "validation": {
      "isValid": true,
      "message": "تغییر موجودی معتبر است",
      "severity": "success"
    }
  }
}
```

**Response (خطا):**
```json
{
  "success": false,
  "error": "پیام خطا"
}
```

### POST /api/login

**Request:**
```json
{
  "debug": false
}
```

**Response:**
```json
{
  "success": true,
  "message": "لاگین با موفقیت انجام شد. Session ذخیره شد."
}
```

---

## 💡 نکات مهم

1. **اولین بار**: حتماً لاگین کنید
2. **Session**: بعد از لاگین، session ذخیره می‌شود
3. **Headless**: به صورت پیش‌فرض فعال است (مرورگر مخفی)
4. **Debug**: برای دیباگ، تیک "نمایش مرورگر" را بزنید
5. **تاریخچه**: در localStorage مرورگر ذخیره می‌شود
6. **موجودی**: موجودی به صورت خودکار پس از هر معامله به‌روزرسانی می‌شود
7. **اعتبارسنجی**: سیستم به صورت خودکار تغییر موجودی را با نوع معامله مقایسه می‌کند

---

## 🔄 به‌روزرسانی

برای به‌روزرسانی:
```bash
git pull
npm install
npm run build
npm run dashboard
```

---

## 📞 پشتیبانی

برای مشکلات:
1. بررسی این راهنما
2. بررسی [README اصلی](../../README.md)
3. بررسی [دفترچه گزارش](../../development/journal.md) برای تغییرات اخیر

