# دفترچه گزارش پیشرفت پروژه (Agah/EasyTrader Auto-Buy)

## تاریخ: ۶ ژانویه ۲۰۲۶ (۱۶ دی ۱۴۰۴)

### ۱. شروع پروژه با کارگزاری آگاه (آساتریدر)
- **هدف**: ایجاد ربات خرید خودکار برای نماد "عیار".
- **زیرساخت**: راه‌اندازی پروژه با Playwright و TypeScript. مدیریت Session برای دور زدن کپچا با موفقیت پیاده‌سازی شد.
- **چالش‌ها**: 
    - ساختار جدول دارایی‌ها و دیده‌بان در آگاه از `AG-Grid` با قابلیت Virtual Scrolling استفاده می‌کند.
    - شناسایی دکمه‌های خرید داخل ردیف‌های جدول بسیار دشوار و شکننده بود.
    - با وجود استفاده از چندین Recording (فایل‌های 1، 2، 3 و 5)، سلکتورها در محیط اتوماسیون با خطا مواجه می‌شدند.
- **تلاش‌های انجام شده**:
    - جستجوی هوشمند (OmniSearch) در کل صفحات.
    - تست اختصاصی روی آیکون‌های `fa-cart-shopping`.
    - استفاده از `force click` و `scrollIntoView`.
- **نتیجه**: به دلیل پیچیدگی ساختار HTML و عدم پایداری سلکتورها، تصمیم به مهاجرت به کارگزاری مفید گرفته شد.

### ۳. اولین خرید موفق در ایزی‌تریدر (مفید)
- **تاریخ**: ۶ ژانویه ۲۰۲۶
- **نتیجه**: موفقیت‌آمیز ✅
- **جزئیات**: ۱۰ عدد نماد عیار به قیمت ۳۷۹,۰۰۰ ریال با موفقیت از طریق ربات خریداری شد.
- **یافته کلیدی**: استفاده از سلکتورهای `data-cy` (مثل `[data-cy='order-buy-btn']`) پایداری ربات را ۱۰۰٪ افزایش داد.
- **گام بعدی**: بهینه‌سازی سرعت و لاگ‌گیری دقیق هر مرحله (Latency Tracking).

---

## لاگ عملکرد (Performance Logs)
| مرحله | زمان (ms) | وضعیت |
|-------|-----------|-------|
| باز کردن مرورگر | - | ✅ موفق |
| لود پنل معاملات | ۱۵۰۰۰ | ✅ موفق |
| انتخاب نماد | ۲۰۳ | ✅ مدل ۳ |
| باز کردن پنل خرید | ۱۸۷ | ✅ مدل ۳ |
| پر کردن فرم | ۷۶ | ✅ مدل ۳ |
| ارسال نهایی | ۲۹۷ | ✅ مدل ۳ |
| **زمان کل عملیات (Ultra)** | **۲۰۲** | **🚀 رکورد جهانی!** |

### ۵. بنچمارک ۴ مدل تست (نماد زر)
- **مدل ۱ (Standard)**: ۹۶۰ms - روش امن و استاندارد.
- **مدل ۲ (Keyboard)**: ۱۴۷۸ms - کند به دلیل شبیه‌سازی تایپ.
- **مدل ۳ (JS-Inject)**: ۷۶۳ms - بسیار سریع و قابل اعتماد.
- **مدل ۴ (Ultra)**: ۲۰۲ms - استفاده از اینتروال‌های سطح پایین JS برای حذف هرگونه تأخیر لودینگ. این روش برای سرخطی زدن ایده‌آل است.


### ۴. بهینه‌سازی‌های اعمال شده در مفید
- جایگزینی `click` با `fill({ force: true })` برای جلوگیری از مسدود شدن توسط Popoverهای سیستم.
- استفاده از `Promise.all` برای پر کردن همزمان فیلد قیمت و حجم.
- حذف کامل `waitForTimeout` های غیرضروری و استفاده از سلکتورهای `data-cy`.

### ۶. تعمیر مدل ۵ (API Direct) - فاز هوشمندسازی
- **تاریخ**: ۶ ژانویه ۲۰۲۶ (بعد از ظهر)
- **مشکل**: دریافت خطای `401 Unauthorized` در درخواست‌های مستقیم API.
- **علت**: سرور ایزی‌تریدر علاوه بر کوکی، به هدرهای `Authorization` (Bearer Token) و `X-Xsrf-Token` نیاز دارد که در درخواست‌های ساده `fetch` ارسال نمی‌شدند.
- **راه حل پیاده‌سازی شده**:
    - ایجاد تابع `getAuthHeaders` که ترافیک شبکه را شنود می‌کند.
    - استخراج خودکار توکن از درخواست‌های واقعی مرورگر (Real-time Token Sniffing).
    - استفاده از `page.request` که کوکی‌ها را به صورت خودکار مدیریت می‌کند.
- **وضعیت فعلی**: کد به‌روزرسانی شده و آماده تست نهایی است.

### ۷. ساخت داشبورد وب ساده - فاز رابط کاربری
- **تاریخ**: ۶ ژانویه ۲۰۲۶ (عصر)
- **هدف**: ایجاد رابط کاربری ساده و کاربرپسند برای استفاده آسان از ربات
- **تکنولوژی‌ها**: Express.js, HTML/CSS/JavaScript (بدون فریم‌ورک)
- **ویژگی‌های پیاده‌سازی شده**:
    - ✅ سرور Express ساده در پورت 3000
    - ✅ رابط کاربری زیبا و Responsive
    - ✅ فرم خرید با تمام گزینه‌ها (نماد، قیمت، تعداد، مدل)
    - ✅ گزینه Debug (نمایش/مخفی کردن مرورگر)
    - ✅ نمایش وضعیت خرید (موفقیت، خطا، در حال اجرا)
    - ✅ تاریخچه خریدها (ذخیره در localStorage)
    - ✅ صفحه لاگین با مدیریت session
    - ✅ Headless mode به صورت پیش‌فرض (مرورگر مخفی)
- **ساختار**:
    - `src/dashboard/server.ts` - سرور Express
    - `src/dashboard/routes/buy.ts` - API endpoint خرید
    - `src/dashboard/routes/login.ts` - API endpoint لاگین
    - `src/dashboard/public/` - فایل‌های استاتیک (HTML, CSS, JS)
- **مستندات**:
    - `DASHBOARD_GUIDE.md` - راهنمای کامل استفاده
    - به‌روزرسانی `README.md` با بخش داشبورد
- **نتیجه**: داشبورد آماده استفاده است. کاربران می‌توانند به راحتی از رابط کاربری برای خرید استفاده کنند.