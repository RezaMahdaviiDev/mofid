# راهنمای استفاده از داشبورد

## 🚀 شروع سریع

### 1. اجرای داشبورد

```bash
npm run dashboard
```

### 2. باز کردن در مرورگر

به آدرس زیر بروید:
```
http://localhost:3000
```

---

## 📋 ویژگی‌ها

### فرم خرید

- **نماد**: نام نماد مورد نظر (مثلاً: زر)
- **قیمت**: قیمت خرید (مثلاً: 590000)
- **تعداد**: تعداد سهم (مثلاً: 2)
- **مدل خرید**: انتخاب یکی از 4 مدل:
  - مدل ۱: Standard (۹۶۰ms) - امن و پایدار
  - مدل ۳: JS Injection (۷۶۳ms) ⭐ - توصیه می‌شود
  - مدل ۴: Ultra (۲۰۲ms) 🏆 - سریع‌ترین
  - مدل ۵: API (۳۶۴ms) 🔮 - در حال توسعه
- **نمایش مرورگر**: برای دیباگ (تیک بزنید تا مرورگر قابل مشاهده باشد)

### نمایش وضعیت

- **آماده**: آماده برای خرید
- **در حال پردازش**: در حال ارسال درخواست
- **موفقیت**: ✅ خرید با موفقیت انجام شد
- **خطا**: ❌ پیام خطا

### تاریخچه خریدها

- نمایش 10 خرید آخر
- شامل: نماد، قیمت، تعداد، زمان، مدت زمان اجرا
- ذخیره در localStorage مرورگر

---

## 🔐 لاگین

### اولین بار

1. کلیک روی دکمه "🔐 لاگین"
2. مرورگر باز می‌شود (یا مخفی است اگر debug غیرفعال باشد)
3. در مرورگر باز شده، به صورت دستی لاگین کنید
4. بعد از 30 ثانیه، session ذخیره می‌شود
5. در خریدهای بعدی نیازی به لاگین نیست

### نکات مهم

- Session در `.user-data/easy/` ذخیره می‌شود
- اگر session منقضی شد، دوباره لاگین کنید
- برای دیباگ، تیک "نمایش مرورگر" را بزنید

---

## 🎯 گزینه Debug

### حالت عادی (Headless)

- مرورگر مخفی است
- سریع‌تر
- مصرف منابع کمتر
- مناسب برای استفاده واقعی

### حالت Debug

- مرورگر قابل مشاهده است
- می‌توانید مراحل را ببینید
- مناسب برای دیباگ و تست
- کندتر از حالت عادی

**نحوه فعال‌سازی:**
- تیک "نمایش مرورگر (برای دیباگ)" را در فرم خرید بزنید
- یا هنگام لاگین، تیک را بزنید

---

## 🐛 عیب‌یابی

### مشکل: سرور اجرا نمی‌شود

**راه‌حل:**
```bash
# بررسی پورت 3000
netstat -ano | findstr :3000

# اگر پورت اشغال است، در server.ts پورت را تغییر دهید
```

### مشکل: خطای 401 در خرید

**علت**: Session منقضی شده یا لاگین نشده

**راه‌حل:**
1. کلیک روی دکمه "لاگین"
2. دوباره لاگین کنید
3. خرید را دوباره امتحان کنید

### مشکل: مرورگر باز نمی‌شود

**راه‌حل:**
```bash
# نصب مرورگرهای Playwright
npx playwright install chromium
```

### مشکل: خطای "Cannot find module"

**راه‌حل:**
```bash
# کامپایل مجدد
npm run build

# نصب وابستگی‌ها
npm install
```

---

## 📊 API Endpoints

### POST /api/buy

**Request:**
```json
{
  "symbol": "زر",
  "price": 590000,
  "quantity": 2,
  "model": "3",
  "debug": false
}
```

**Response (موفق):**
```json
{
  "success": true,
  "message": "خرید با موفقیت انجام شد",
  "duration": 763,
  "order": {
    "symbol": "زر",
    "price": 590000,
    "quantity": 2
  }
}
```

**Response (خطا):**
```json
{
  "success": false,
  "error": "پیام خطا"
}
```

### POST /api/login

**Request:**
```json
{
  "debug": false
}
```

**Response:**
```json
{
  "success": true,
  "message": "لاگین با موفقیت انجام شد. Session ذخیره شد."
}
```

---

## 💡 نکات مهم

1. **اولین بار**: حتماً لاگین کنید
2. **Session**: بعد از لاگین، session ذخیره می‌شود
3. **Headless**: به صورت پیش‌فرض فعال است (مرورگر مخفی)
4. **Debug**: برای دیباگ، تیک "نمایش مرورگر" را بزنید
5. **تاریخچه**: در localStorage مرورگر ذخیره می‌شود

---

## 🔄 به‌روزرسانی

برای به‌روزرسانی:
```bash
git pull
npm install
npm run build
npm run dashboard
```

---

## 📞 پشتیبانی

برای مشکلات:
1. بررسی این راهنما
2. بررسی `README.md`
3. بررسی `JOURNAL.md` برای تغییرات اخیر

